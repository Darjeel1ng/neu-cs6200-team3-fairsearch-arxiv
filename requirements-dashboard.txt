# Direct deps.
streamlit==1.38.0
plotly==5.24.1
pyyaml==6.0.2

# pandas 2.2.2 wheels are compiled against the numpy 1.x C-API; numpy 2.x
# support landed in pandas 2.2.3. pandas only declares `numpy>=1.26.0` with no
# upper bound, so leaving numpy unpinned resolved it to 2.5.2 and the container
# died with SIGSEGV (exit 139) mid-render -- no Python traceback, because the
# crash is in the native layer. Import and simple ops survive the mismatch,
# which is why it looked intermittent. Pin all three together; bump as a set.
pandas==2.2.3
numpy==2.1.3
pyarrow==17.0.0
